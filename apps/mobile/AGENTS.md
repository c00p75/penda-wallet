# Agent notes (Penda mobile)

## Copy style

- **Never use the em dash (`—`).** Prefer periods, commas, or colons.
- Before finishing UI changes, search touched files for `—`.

Project-wide rule: `../../.cursor/rules/no-em-dash.mdc` and root `AGENTS.md`.
