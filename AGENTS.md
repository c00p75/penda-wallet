# Agent notes (Penda)

## Copy style

- **Never use the em dash (`—`).** It reads as AI-generated and hurts trust.
- Prefer periods, commas, or colons so sentences still make sense.
- Before finishing UI or prompt changes, search touched files for `—`.

See also: `.cursor/rules/no-em-dash.mdc`.
